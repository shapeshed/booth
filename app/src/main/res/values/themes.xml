<resources>

    <!-- Base application theme. The real colours come from MaterialExpressiveTheme in Compose;
         this only styles the window before Compose draws. -->
    <style name="Theme.Booth" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Launch theme via androidx.core:core-splashscreen. -->
    <style name="Theme.Booth.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/booth_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Booth</item>
    </style>

</resources>
